# 沙箱环境：pnpm 的 store / cache / state 全部重定向到项目内，避免写入用户目录被拒
store-dir=.pnpm-store
cache-dir=.pnpm-cache
state-dir=.pnpm-state

# 中国大陆网络下的稳定性设置（不改动 registry，仅在失败时重试）
fetch-retries=3
fetch-retry-maxtimeout=120000
